import Ember from 'ember';
import layout from './template';

/**
 * @module ember-osf
 * @submodule components
 */

/**
 * Display the donate banner.  Has an automated script to determine what the banner should look like
 * based on the the current date.
 *
 * Sample usage:
 * ```handlebars
 * {{osf-navbar
 *   loginAction=loginAction
 * }}
 * ```
 *
 * @class donate-banner
 */
export default Ember.Component.extend({

    
});
