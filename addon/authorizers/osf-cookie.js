import Base from 'ember-simple-auth/authorizers/base';

export default Base.extend({
    authorize(/*data, block*/) {
        // TODO: Determine if xhr.withCredentials is needed and implement accordingly
    }
});
