import ajaxHelper from 'dummy/utils/ajax-helper';
import { module, test } from 'qunit';

module('Unit | Utility | ajax helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ajaxHelper();
  assert.ok(result);
});
