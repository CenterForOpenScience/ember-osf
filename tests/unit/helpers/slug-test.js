
import { slug } from 'dummy/helpers/slug';
import { module, test } from 'qunit';

module('Unit | Helper | slug');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = slug([42]);
  assert.ok(result);
});

