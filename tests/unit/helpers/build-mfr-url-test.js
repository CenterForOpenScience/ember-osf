import { buildMfrUrl } from 'dummy/helpers/build-mfr-url';
import { module, test } from 'qunit';

module('Unit | Helper | build mfr url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = buildMfrUrl([42]);
  assert.ok(result);
});
