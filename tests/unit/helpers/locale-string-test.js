
import { localeString } from 'dummy/helpers/locale-string';
import { module, test } from 'qunit';

module('Unit | Helper | locale string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = localeString([42]);
  assert.ok(result);
});

